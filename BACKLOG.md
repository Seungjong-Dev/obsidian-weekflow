# WeekFlow Backlog

코드 수정·기능 추가 백로그. CeteOS 세션에서 발견된 항목을 여기에 기록한다.

---

## 버그

- [x] `log:add` 시 시간순 삽입 — 현재 마크다운에 append만 하는데, time 기준으로 올바른 위치에 삽입하도록 수정 (2026-04-13) ✅ 2026-04-13

## 기능 요청

- [ ] 키보드만으로 블럭 생성·조작 — nvim 단축키 차용 (hjkl 이동, i 삽입, d 삭제 등). 마우스 없이 타임라인 조작 (2026-04-13)
- [ ] 인박스에 due date·중요도(priority) 필드 지원 추가 (2026-04-12)
- [ ] 리뷰 패널이 하단으로 스크롤되게 — 로그 append 시 항상 하단에 추가되므로 (2026-04-13)
- [ ] 인라인 에디터에서 `@`로 시간 지정 — `#`로 카테고리 선택하는 것처럼, `@14:00`으로 시간 지정 가능하게 (2026-04-13)
- [ ] 타임라인 블럭 연계 (linked blocks) — 한 작업이 점심 등으로 중간에 끊길 때, 같은 작업의 두 블럭을 연계해서 생성/관리. 같은 작업으로 묶이고 통계도 합산. UI에서 시각적으로 연결 표시 (2026-04-14)
- [ ] 인라인 에디터에서 `+`로 인박스 아이템 선택 — `+` 접두사 입력 시 현재 인박스 목록이 autocomplete로 뜨고 선택 시 타임라인 블럭 content로 주입 + 인박스에서 제거 옵션. prefix `/`는 향후 slash command(예: `/template`, `/defer`)를 위해 비워둠 (2026-04-16)
- [x] `weekflow:settings` CLI — 시작요일 등 플러그인 설정을 외부에서 조회할 수 있는 명령. 회고·브리핑·계획 스킬이 시작요일을 동적으로 가져오는 데 필요 (2026-04-13) ✅ 2026-04-13
- [x] 주간 노트 CLI 지원 — `weekflow:weekly` (이번 주 위클리 노트 접근), `weekflow:weekly:review:write` (위클리 Review 작성). weekNumbering 설정 추가 (ISO 8601 등). 시작요일 설정은 기존 활용 (2026-04-13) ✅ 2026-04-13
